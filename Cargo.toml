[package]
edition = "2021"
name = "zoe"
version = "0.0.2"

[dependencies]
ahash = { version = "*", default-features = false, features = ["std"] }
atoi = "*"
itoa = "*"
rand_xoshiro = "*"

[profile.release]
strip = true
lto = "thin"
