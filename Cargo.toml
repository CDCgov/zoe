[package]
edition = "2021"
name = "zoe"
version = "0.1.0"

[dependencies]
ahash = "*"
atoi = "*"
lazy_static = "*"
rand_xoshiro = "*"
regex = { version = "*", default-features = false, features = [
    "std",
    "perf",
    "unicode-perl",
] }

[profile.release]
strip = true
lto = "thin"
