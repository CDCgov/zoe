stages:
  - build
  - test

zoe-build:
  stage: build
  tags: 
    - docker
    - scicomp
  image: rustlang/rust:nightly-slim
  script:
    - cargo build --verbose

zoe-test:
  stage: test
  tags: 
    - docker
    - scicomp
  image: rustlang/rust:nightly-slim
  script:
    - cargo test --verbose
    